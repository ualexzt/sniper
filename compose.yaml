services:
  recorder:
    build: .
    image: sniper-recorder:local
    container_name: sniper-recorder
    restart: unless-stopped
    init: true
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - ./runtime:/runtime
    command: ["--output", "/runtime/bybit_raw", "--rotate-minutes", "60"]
